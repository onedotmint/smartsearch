# Codex Adapter

Install the shared skill under `~/.codex/skills/smart-search-cli`.

Use the CLI-first workflow from the parent skill. Do not combine it with native
web search or invent a Codex-only provider route.
