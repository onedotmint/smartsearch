# Codex Adapter

Install the shared v1 Skill under `~/.codex/skills/smart-search-cli`.

Use the CLI-first workflow from the parent Skill and parse its single v1 JSON
envelope. Do not combine it with native web search or invent a client-only
provider route.
