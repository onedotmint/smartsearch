# v1.0.2 — deterministic release preparation

This patch release contains the Trusted Publisher workflow fix from `b0316f9`,
the shared static provider catalog refactor from `a8f9877`, and cleanup of
retired model routes from `202e321`.

The root npm wrapper, independent Pi package, and Python project metadata are
synchronized at `1.0.2`. This candidate is validated offline; no live-provider
validation, publication, tag, or GitHub Release creation is part of this
preparation.
